# chrony.conf — NTP server for ids_net clients

# Serve time to the ids_net subnet
allow 172.20.0.0/24

# Use local clock as reference when no upstream is reachable
local stratum 10

# Bind to all interfaces
bindaddress 0.0.0.0
